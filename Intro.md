IN this file we will have an introductory part of the K nearest neighbour algorithm 

-->it work as :

1 -> WE consider a value of K using cross validation method 
2 -> We calculate the EUCLIDEIEN DISTANCE with the all data points of the input data point
3 -> We sort those all distances 
4 -> We chose majority of class.
5 ->  which class have majority in the K nearest neighbour we choose that class for the input data points.


# for each and every value of k we calculate the accuracy and other parameter then when we get a proper high parameter we use that values of the K.